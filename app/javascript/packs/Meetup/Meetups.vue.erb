<template>
    <div>
        <v-layout row wrap>
            <v-flex xs12 sm10 md8 offset-md2>
                <v-card class="info mb-2" v-for="meetup in meetups" :key="meetup.id">
                    <v-container>
                        <v-layout row>
                            <v-flex xs5>
                            <v-card-media
                            :src="meetup.imageUrl"
                            height="130px"
                            contain>
                    </v-card-media>
                </v-flex>
        <v-flex xs7 sm8 md-9>
              <v-card-title primary-title>
                  <div>
                  <h5 class="white--text">{{ meetup.title }}</h5>
                  <div>Listen to your favorite artists, whenever and wherever, offline or online</div>
                  <v-btn small flat @click="redirect_to(meetup.id)">
                  <v-icon left>arrow_forward</v-icon>
                  Check it out</v-btn>
                  </div>

                               </v-card-title>
                            </v-flex>
                        </v-layout>
                    </v-container>
                </v-card>
            </v-flex>
        </v-layout>
    </div>
</template>

<script>
  export default {
  	methods: {
  		redirect_to(id){
  			this.$router.push('/meetup/' + id)
  		}
  	},
  	computed: {
  		meetups() {
  			return this.$store.getters.loadedMeetups
  		}
  	}
  }
</script>